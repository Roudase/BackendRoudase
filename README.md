
# BackendRoudase — Лабораторні роботи 1–2 (TypeScript / Express / Docker)

Цей проєкт реалізує базовий REST API на **TypeScript + Express** для дисципліни “Технології серверного програмного забезпечення”:

- **Лабораторна робота 1** – базовий ендпоінт `/healthcheck`, Docker/Docker Compose, деплой.
- **Лабораторна робота 2** – REST API для обліку витрат (користувачі, категорії, записи), тестування через Postman.

---

## 🔧 Технології

- Node.js
- TypeScript
- Express
- Docker / docker-compose
- Postman (колекції + environments)

---

## 🚀 Встановлення та запуск локально

### 1. Встановлення залежностей

```bash
npm install
```

### 2. Запуск у режимі розробки

```bash
npm run dev
```

За замовчуванням сервер запускається на порту `8080`.

---

## ✅ Лабораторна робота 1 — /healthcheck

### Ендпоінт

`GET /healthcheck`

Приклад відповіді:

```json
{
  "date": "2025-12-09T12:00:00.000Z",
  "status": "ok"
}
```

Це реалізує healthcheck згідно методички: сервіс повертає HTTP 200 і JSON з поточною датою та статусом сервісу.

---

## 📦 Лабораторна робота 2 — REST API для обліку витрат

У межах ЛР2 реалізовано API для:

- створення та отримання користувачів;
- створення та отримання категорій витрат;
- створення та отримання записів про витрати;
- фільтрації записів за користувачем та/або категорією.

> Згідно з вимогами ЛР2, дані зберігаються **в пам’яті** (in-memory), без бази даних.

### 1. Користувачі

#### Структура даних (User)

```ts
{
  id: number;
  name: string;
}
```

#### Ендпоінти

- `POST /user` – створити користувача  
  **Body** (JSON):

  ```json
  {
    "name": "John Doe"
  }
  ```

  **Коди відповідей**:
  - `201` – користувача створено
  - `400` – якщо не передано `name`

- `GET /user/<user_id>` – отримати користувача за id  
  **Приклад:** `GET /user/1`  
  **Коди відповідей**:
  - `200` – знайдено
  - `400` – некоректний id
  - `404` – користувача не знайдено

- `DELETE /user/<user_id>` – видалити користувача  
  Разом із користувачем видаляються всі пов’язані записи про витрати.  
  **Коди відповідей**:
  - `204` – успішне видалення
  - `400` – некоректний id
  - `404` – користувача не знайдено

- `GET /users` – отримати список усіх користувачів  
  **Код відповіді:** `200`

---

### 2. Категорії витрат

#### Структура даних (Category)

```ts
{
  id: number;
  name: string;
}
```

#### Ендпоінти

- `POST /category` – створити категорію  
  **Body** (JSON):

  ```json
  {
    "name": "Food"
  }
  ```

  **Коди відповідей**:
  - `201` – категорію створено
  - `400` – якщо не передано `name`

- `GET /category` – отримати список категорій  
  **Код відповіді:** `200`

- `DELETE /category` – видалити категорію за id (через query-параметр)  
  **Приклад:** `DELETE /category?id=1`  
  **Коди відповідей**:
  - `204` – успішне видалення
  - `400` – якщо не передано `id` або некоректний `id`
  - `404` – категорію не знайдено

---

### 3. Записи витрат (Record)

#### Структура даних (ExpenseRecord)

```ts
{
  id: number;
  userId: number;
  categoryId: number;
  amount: number;
  createdAt: string; // ISO-рядок дати
}
```

#### Ендпоінти

- `POST /record` – створити запис витрати  
  **Body** (JSON):

  ```json
  {
    "userId": 1,
    "categoryId": 1,
    "amount": 150.5
  }
  ```

  **Перевірки**:
  - існування користувача (`userId`);
  - існування категорії (`categoryId`);
  - числові значення для всіх полів.

  **Коди відповідей**:
  - `201` – запис створено
  - `400` – якщо дані некоректні або користувач/категорія не існують

- `GET /record/<record_id>` – отримати запис за id  
  **Приклад:** `GET /record/1`  
  **Коди відповідей**:
  - `200` – знайдено
  - `400` – некоректний id
  - `404` – запис не знайдено

- `DELETE /record/<record_id>` – видалити запис за id  
  **Приклад:** `DELETE /record/1`  
  **Коди відповідей**:
  - `204` – успішне видалення
  - `400` – некоректний id
  - `404` – запис не знайдено

- `GET /record?user_id=&category_id=` – фільтрація записів  
  Підтримуються такі варіанти виклику:

  - `GET /record?user_id=1` – всі записи користувача `1`;
  - `GET /record?category_id=1` – всі записи категорії `1`;
  - `GET /record?user_id=1&category_id=1` – записи користувача `1` в категорії `1`.

  **ВАЖЛИВО:**  
  Якщо не передано **жодного** з параметрів `user_id` або `category_id`, ендпоінт повертає:

  - `400` – помилка із повідомленням, що потрібен хоча б один параметр.

---

## 🧪 Postman: колекція та environments

Для тестування API підготовлено:

- **Колекція запитів:**  
  `BackendRoudase-lab1-2.postman_collection.json`
- **Environments:**
  - `BackendRoudase-local.postman_environment.json` – для локального запуску
  - `BackendRoudase-render.postman_environment.json` – для прод-оточення (Render)

### Використання

1. Імпортувати колекцію та environments у Postman.
2. Обрати відповідне середовище (Local або Render).
3. Послідовно викликати:
   - `POST /user`
   - `POST /category`
   - `POST /record`
   - `GET /record` з різними параметрами (`user_id`, `category_id`).

---

## 🐳 Збірка та запуск через Docker

### 1. Збірка образу

```bash
docker build -t backend-roudase:latest .
```

### 2. Запуск контейнера

```bash
docker run -it --rm -p 8080:8080 backend-roudase:latest
```

Після цього ендпоінти будуть доступні за адресою:

```text
http://localhost:8080/healthcheck
http://localhost:8080/user
http://localhost:8080/category
http://localhost:8080/record
```

---

## 🐙 Запуск через docker-compose

```bash
docker-compose build
docker-compose up
```

---

## ☁️ Деплой на Render

1. Завантажити репозиторій на GitHub.
2. Створити сервіс типу **Web Service** на [render.com](https://render.com/).
3. Обрати репозиторій, гілку та задати:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `node dist/server.js`
4. Після успішного деплою перевірити healthcheck:

```text
https://<your-render-service>.onrender.com/healthcheck
```

---

## 📂 Структура проєкту

```text
src/
  app.ts         # Ініціалізація Express-застосунку
  routes.ts      # Маршрути: /healthcheck, /user, /users, /category, /record
server.ts        # Точка входу, запуск сервера
Dockerfile       # Опис Docker-образу
docker-compose.yml # Конфігурація для локального запуску в контейнері
tsconfig.json    # Налаштування TypeScript
package.json     # Залежності та скрипти
.gitignore       # Ігноровані файли та папки
.dockerignore    # Ігноровані файли та папки для Docker
```

---

## 🧾 Примітки щодо git

- Коміти мають бути **структурованими і осмисленими**.
- Приклад логічних кроків:
  - `feat: init BackendRoudase project`
  - `feat: add /healthcheck endpoint`
  - `feat: implement expenses REST API (lab 2)`
  - `chore: add Docker and docker-compose`
  - `docs: update README for labs 1–2`
